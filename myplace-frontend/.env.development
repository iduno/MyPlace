# Vite development environment variables
# Base URL the frontend will use for API calls in development.
# Typically point to the Vite dev server proxy target path or direct backend.
VITE_API_BASE_URL=/api